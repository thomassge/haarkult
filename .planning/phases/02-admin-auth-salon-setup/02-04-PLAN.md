---
phase: 02-admin-auth-salon-setup
plan: 04
type: execute
wave: 4
depends_on: [02-03]
files_modified:
  - app/admin/_components/admin-shell.tsx
  - app/admin/page.tsx
  - lib/booking/setup-validation.ts
  - tests/phase-2/admin-dashboard-navigation.test.ts
  - tests/phase-2/availability-exceptions.test.ts
autonomous: true
gap_closure: true
requirements: [ADMN-06, STAF-02, STAF-04]
requirements_addressed: [ADMN-06, STAF-02, STAF-04]
must_haves:
  truths:
    - "Every admin dashboard card links to an existing Phase 2 admin setup route."
    - "The Leistungen dashboard card sends admins to the existing stylist/service-assignment setup route instead of a missing route."
    - "Malformed timed availability exception strings are rejected during validation before persistence actions can write them."
    - "Berlin datetime-local timed exception conversion rejects impossible or invalid parsed dates."
  artifacts:
    - path: "app/admin/_components/admin-shell.tsx"
      provides: "Admin dashboard card definitions with no missing route hrefs"
    - path: "app/admin/page.tsx"
      provides: "Dashboard card status mapping that does not preserve a missing /admin/leistungen target"
    - path: "lib/booking/setup-validation.ts"
      provides: "Exception date parsing that rejects Invalid Date outputs"
    - path: "tests/phase-2/admin-dashboard-navigation.test.ts"
      provides: "Regression coverage that admin dashboard cards do not link to missing setup routes"
    - path: "tests/phase-2/availability-exceptions.test.ts"
      provides: "Regression coverage for malformed timed exception input rejection"
  key_links:
    - from: "app/admin/_components/admin-shell.tsx"
      to: "app/admin/stylisten/page.tsx"
      via: "Leistungen card href"
      pattern: "href: \"/admin/stylisten\""
    - from: "lib/booking/setup-validation.ts"
      to: "lib/booking/setup-actions.ts"
      via: "normalizeAvailabilityException before saveAvailabilityExceptionAction persistence"
      pattern: "normalizeAvailabilityException"
---

<objective>
Close Phase 2 verification gaps without changing the completed base plans.

Purpose: Phase 2 cannot verify cleanly while the admin dashboard contains a 404 setup target and malformed timed exception input can reach persistence as `Invalid Date`.
Output: One dashboard navigation regression fix, one exception validation regression fix, and focused tests proving both failed verification truths are now satisfied.
</objective>

<execution_context>
@C:/Users/neyma/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/neyma/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-admin-auth-salon-setup/02-CONTEXT.md
@.planning/phases/02-admin-auth-salon-setup/02-VERIFICATION.md
@.planning/phases/02-admin-auth-salon-setup/02-REVIEW.md
@.planning/phases/02-admin-auth-salon-setup/02-02-SUMMARY.md
@.planning/phases/02-admin-auth-salon-setup/02-03-SUMMARY.md
@app/admin/_components/admin-shell.tsx
@app/admin/page.tsx
@lib/booking/setup-validation.ts
@tests/phase-2/availability-exceptions.test.ts

<interfaces>
`adminDashboardCards` is exported from `app/admin/_components/admin-shell.tsx` and consumed by `app/admin/page.tsx`.
Current Phase 2 implemented service assignment inside `/admin/stylisten`; no `/admin/leistungen` route exists.
`normalizeAvailabilityException(input)` in `lib/booking/setup-validation.ts` is the validation/normalization boundary used before exception persistence.
`parseExceptionDate(input)` currently handles ISO strings, JavaScript `Date` inputs, and `datetime-local` strings that should be interpreted as Europe/Berlin local time.
</interfaces>
</context>

<requirements>
ADMN-06: Salon staff can update core salon booking setup from the admin area.
STAF-02: Salon staff can assign which services each stylist can perform.
STAF-04: Salon staff can set one-off blocked times, breaks, vacations, or availability exceptions for each stylist.

Decision coverage:
- D-08 and D-24: Keep dashboard frames/cards, but every card must route to a real focused setup area.
- D-13, D-14, D-15, D-16: Service assignment remains under the existing stylist setup flow; do not create a duplicate service-management model.
- D-19 and D-20: Timed exceptions must have reliable persisted windows and malformed values must be rejected before persistence.

Deferred/advisory exclusions:
- Do not implement WR-02 stale JWT active-claim revocation in this gap-closure plan. It is advisory debt, not one of the required verification gaps.
</requirements>

<must_haves>
- Dashboard navigation truth: no dashboard card href points to `/admin/leistungen` unless an actual `app/admin/leistungen/page.tsx` route is added. This plan uses the smallest coherent fix: repoint `Leistungen` to `/admin/stylisten`.
- Exception validation truth: malformed timed `startAt` or `endAt` values such as `not-a-date` fail validation/normalization with a controlled German error before `saveAvailabilityExceptionAction` can persist.
- Berlin datetime-local truth: `YYYY-MM-DDTHH:mm` strings still convert through the Europe/Berlin branch, but impossible values that produce `Invalid Date` are rejected instead of returned.
</must_haves>

<tasks>

<task id="02-04-01" type="auto" tdd="true">
  <name>Task 1: Repoint the Leistungen dashboard card to the existing setup route</name>
  <read_first>
    <file>app/admin/_components/admin-shell.tsx</file>
    <file>app/admin/page.tsx</file>
    <file>app/admin/stylisten/page.tsx</file>
    <file>tests/phase-2/admin-auth.test.ts</file>
    <file>tests/phase-2/service-assignment.test.ts</file>
  </read_first>
  <files>app/admin/_components/admin-shell.tsx, app/admin/page.tsx, tests/phase-2/admin-dashboard-navigation.test.ts</files>
  <action>
    Create `tests/phase-2/admin-dashboard-navigation.test.ts` first. The test must read the relevant source files and assert:
    - `app/admin/_components/admin-shell.tsx` no longer contains `href: "/admin/leistungen"`.
    - the dashboard still contains a `Leistungen` card.
    - the `Leistungen` card href is `/admin/stylisten`.
    - `app/admin/page.tsx` does not filter for or branch on `/admin/leistungen`.
    - all static admin dashboard card hrefs resolve to existing route files: `/admin/stylisten`, `/admin/zeiten`, and `/admin/ausnahmen`.

    Then update `app/admin/_components/admin-shell.tsx` so the `Leistungen` dashboard card uses `href: "/admin/stylisten"` and keeps German copy that explains service assignment happens per stylist.
    Update `app/admin/page.tsx` so `requiredSetupRoutes` and card filtering/status mapping do not reference `/admin/leistungen`. Preserve a separate `Leistungen` status by identifying the card title or by giving the duplicated `/admin/stylisten` service card a stable status path that does not require a missing href. The status must still show `${overview.counts.assignedServices} Zuordnungen` for the `Leistungen` card.
    Do not add `app/admin/leistungen/page.tsx`; the required smallest coherent fix is repointing to the existing `/admin/stylisten` route.
  </action>
  <acceptance_criteria>
    - `rg '"/admin/leistungen"' app/admin tests/phase-2` returns no production references except inside historical planning artifacts, if searched from the repo root with `.planning` excluded.
    - `tests/phase-2/admin-dashboard-navigation.test.ts` contains an assertion that `Leistungen` resolves to `/admin/stylisten`.
    - `app/admin/page.tsx` no longer contains `/admin/leistungen`.
    - `app/admin/_components/admin-shell.tsx` still contains the German card title `Leistungen`.
    - `app/admin/_components/admin-shell.tsx` contains at least one `href: "/admin/stylisten"` for the `Leistungen` card and no card points at a missing route.
  </acceptance_criteria>
  <verify>
    <automated>npm.cmd run test:unit -- tests/phase-2/admin-dashboard-navigation.test.ts tests/phase-2/service-assignment.test.ts tests/phase-2/setup-completion.test.ts</automated>
    <automated>npm.cmd run lint</automated>
  </verify>
  <done>The admin dashboard has no 404 card targets and the Leistungen card opens the existing stylist/service-assignment setup screen.</done>
</task>

<task id="02-04-02" type="auto" tdd="true">
  <name>Task 2: Reject malformed timed availability exception dates before persistence</name>
  <read_first>
    <file>lib/booking/setup-validation.ts</file>
    <file>lib/booking/setup-actions.ts</file>
    <file>tests/phase-2/availability-exceptions.test.ts</file>
    <file>tests/phase-2/weekly-availability.test.ts</file>
  </read_first>
  <files>lib/booking/setup-validation.ts, tests/phase-2/availability-exceptions.test.ts</files>
  <action>
    Add failing tests to `tests/phase-2/availability-exceptions.test.ts` before changing production code. Cover at least:
    - timed exception input with `startAt: "not-a-date"` throws `Bitte einen gueltigen Zeitraum angeben.`
    - timed exception input with `endAt: "not-a-date"` throws `Bitte einen gueltigen Zeitraum angeben.`
    - a malformed or impossible Berlin `datetime-local` value that matches the local datetime branch but cannot produce a valid calendar instant is rejected with the same controlled error.
    - valid Berlin `datetime-local` values such as `2026-06-15T10:00` and `2026-06-15T10:30` still normalize successfully.

    Update `lib/booking/setup-validation.ts` so `parseExceptionDate(input)` validates every returned `Date`. For any `Date` created from a string, any input `Date`, and any Date returned by `berlinLocalDateTimeToUtc`, check `Number.isNaN(parsedDate.getTime())` and throw `new Error("Bitte einen gueltigen Zeitraum angeben.")` when invalid.
    Tighten the Berlin `datetime-local` branch so invalid calendar rollovers are not silently accepted. The `YYYY-MM-DDTHH:mm` regex may still detect datetime-local inputs, but the parsed components must round-trip to the same local date, hour, and minute in `Europe/Berlin` before returning the UTC instant. If round-trip validation fails, throw the controlled German error.
    Keep the existing all-day Europe/Berlin normalization semantics and the existing `Das Ende muss nach dem Start liegen.` error for valid dates whose order is inverted.
    Do not alter persistence actions except as needed to preserve imports or tests; this task is about the validation boundary before persistence.
  </action>
  <acceptance_criteria>
    - `lib/booking/setup-validation.ts` contains `Number.isNaN` or equivalent `getTime()` validity checks inside `parseExceptionDate` or a helper used by it.
    - `parseExceptionDate` never returns an `Invalid Date`.
    - `tests/phase-2/availability-exceptions.test.ts` asserts malformed timed strings throw `Bitte einen gueltigen Zeitraum angeben.`
    - valid ISO timestamp strings and valid Berlin `datetime-local` strings still pass normalization.
    - the source test still confirms `saveAvailabilityExceptionAction` is guarded by `requireAdmin()`.
  </acceptance_criteria>
  <verify>
    <automated>npm.cmd run test:unit -- tests/phase-2/availability-exceptions.test.ts tests/phase-2/weekly-availability.test.ts tests/phase-2/setup-completion.test.ts</automated>
    <automated>npm.cmd run lint</automated>
    <automated>npm.cmd run build</automated>
  </verify>
  <done>Malformed timed exception input fails as controlled validation before persistence while valid Berlin and ISO timed windows still normalize correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin dashboard -> route navigation | Admin users follow setup links to privileged operational screens; broken targets can hide required setup workflows. |
| admin browser -> exception Server Action | Untrusted timed exception strings enter server-side validation before database writes. |
| local datetime input -> timestamptz persistence | Browser-local Berlin datetime intent must become a valid, unambiguous UTC instant before storage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | Denial of service | `app/admin/_components/admin-shell.tsx`, `app/admin/page.tsx` | mitigate | Repoint the `Leistungen` card to the existing `/admin/stylisten` route and add a regression test that dashboard cards do not link to missing route files. |
| T-02-14 | Tampering | `lib/booking/setup-validation.ts` | mitigate | Reject malformed timed exception strings and invalid Berlin datetime-local conversions before `saveAvailabilityExceptionAction` can persist schedule overrides. |
| T-02-15 | Repudiation | exception validation errors | accept | Phase 2 does not add audit logging for validation failures; controlled German validation errors are sufficient for this gap closure. |
</threat_model>

<verification>
Run the focused gap-closure checks:
- `npm.cmd run test:unit -- tests/phase-2/admin-dashboard-navigation.test.ts tests/phase-2/availability-exceptions.test.ts tests/phase-2/service-assignment.test.ts tests/phase-2/weekly-availability.test.ts tests/phase-2/setup-completion.test.ts`
- `npm.cmd run lint`
- `npm.cmd run build`

Optional source check:
- `rg '"/admin/leistungen"' app tests --glob '!*.md'` should return no active production/test references unless the executor deliberately added a real `/admin/leistungen` route, which this plan does not ask for.
</verification>

<success_criteria>
Phase 2 gap closure is complete when the admin dashboard no longer links to a missing route, the Leistungen card opens `/admin/stylisten`, malformed timed availability exception inputs fail validation before persistence, and the focused Phase 2 tests, lint, and build pass.
</success_criteria>

<output>
After completion, create `.planning/phases/02-admin-auth-salon-setup/02-04-SUMMARY.md`.
Then run phase gap execution through `/gsd-execute-phase 2 --gaps-only`.
</output>
