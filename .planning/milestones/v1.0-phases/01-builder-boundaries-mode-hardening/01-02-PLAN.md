---
phase: 01-builder-boundaries-mode-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - package.json
  - vitest.config.ts
  - tests/setup.ts
  - tests/phase-1/site-mode.test.ts
  - tests/phase-1/termin-buchen-page.test.tsx
  - app/page.tsx
  - app/termin-buchen/page.tsx
  - lib/home-page.ts
autonomous: true
requirements:
  - MODE-01
  - MODE-02
  - MODE-03
must_haves:
  truths:
    - "In `contact_only`, brochure surfaces do not expose a working booking entry CTA."
    - "In `booking`, brochure surfaces expose `/termin-buchen` and still show only the salon-configured fallback contact options."
    - "Direct visits to `/termin-buchen` in `contact_only` show a friendly fallback page with configured contact channels instead of a working booking flow."
  artifacts:
    - path: "vitest.config.ts"
      provides: "Wave 0 unit/component harness for Phase 1 regressions"
      contains: "test:"
    - path: "tests/phase-1/site-mode.test.ts"
      provides: "Selector regression coverage for `contact_only` and `booking`"
      contains: "describe("
    - path: "tests/phase-1/termin-buchen-page.test.tsx"
      provides: "`/termin-buchen` fallback rendering coverage"
      contains: "render("
    - path: "app/termin-buchen/page.tsx"
      provides: "Mode-aware booking entry or fallback page"
      contains: "content/booking"
  key_links:
    - from: "app/page.tsx"
      to: "lib/site-mode.ts"
      via: "shared booking CTA resolution"
      pattern: "from \"@/lib/site-mode\""
    - from: "app/termin-buchen/page.tsx"
      to: "content/booking.ts"
      via: "fallback copy and visible channels"
      pattern: "from \"@/content/booking\""
    - from: "tests/phase-1/termin-buchen-page.test.tsx"
      to: "app/termin-buchen/page.tsx"
      via: "component render assertions"
      pattern: "from \"@/app/termin-buchen/page\""
---

<objective>
Harden the public mode contract and close the known drift on brochure CTAs plus the `/termin-buchen` fallback surface.

Purpose: Implement D-04, D-05, D-06, D-07, and D-10 with automated checks, because the validation contract marks current coverage as missing.
Output: A Wave 0 test harness, selector regression tests, and route wiring that derives contact visibility and fallback copy from shared config instead of duplicated JSX strings.
</objective>

<execution_context>
@C:/Users/neyma/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/neyma/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/neyma/dev/haarkult/AGENTS.md
@C:/Users/neyma/dev/haarkult/.planning/ROADMAP.md
@C:/Users/neyma/dev/haarkult/.planning/phases/01-builder-boundaries-mode-hardening/01-CONTEXT.md
@C:/Users/neyma/dev/haarkult/.planning/phases/01-builder-boundaries-mode-hardening/01-RESEARCH.md
@C:/Users/neyma/dev/haarkult/.planning/phases/01-builder-boundaries-mode-hardening/01-VALIDATION.md
@C:/Users/neyma/dev/haarkult/content/booking.ts
@C:/Users/neyma/dev/haarkult/lib/site-mode.ts
@C:/Users/neyma/dev/haarkult/app/page.tsx
@C:/Users/neyma/dev/haarkult/app/termin-buchen/page.tsx

<interfaces>
From lib/site-mode.ts (created in 01-01):
```ts
export function isBookingEnabled(...): boolean;
export function getBookingEntryHref(...): string | null;
export function resolveVisibleContactKinds(...): string[];
```

From lib/home-page.ts:
```ts
export type ResolvedPageAction = {
  label: string;
  href: string;
  variant: ButtonVariant;
  external: boolean;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish Wave 0 automated coverage for mode gating and fallback rendering</name>
  <files>package.json, vitest.config.ts, tests/setup.ts, tests/phase-1/site-mode.test.ts, tests/phase-1/termin-buchen-page.test.tsx</files>
  <read_first>
    - package.json
    - .planning/phases/01-builder-boundaries-mode-hardening/01-VALIDATION.md
    - .planning/phases/01-builder-boundaries-mode-hardening/01-RESEARCH.md
    - content/booking.ts
    - lib/site-mode.ts
  </read_first>
  <action>Add the Wave 0 harness the validation artifact expects: a `test:unit` script in `package.json`, `vitest.config.ts`, and `tests/setup.ts` for jsdom and path aliases. Author `tests/phase-1/site-mode.test.ts` to lock selector behavior for both `contact_only` and `booking`, and author `tests/phase-1/termin-buchen-page.test.tsx` to lock the friendly `/termin-buchen` fallback in `contact_only`. Pull expected copy and visible channel behavior from `content/booking.ts` and selector outputs rather than hardcoding stale route text.</action>
  <acceptance_criteria>
    - `rg -n '\"test:unit\"' package.json` returns a match.
    - `Test-Path vitest.config.ts` returns `True`.
    - `Test-Path tests/setup.ts` returns `True`.
    - `Test-Path tests/phase-1/site-mode.test.ts` returns `True`.
    - `Test-Path tests/phase-1/termin-buchen-page.test.tsx` returns `True`.
  </acceptance_criteria>
  <verify>
    <automated>npm.cmd run test:unit -- tests/phase-1/site-mode.test.ts</automated>
  </verify>
  <done>The repo has a fast Phase 1 unit harness and at least the selector contract is automatically checked before route rewiring begins.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire homepage and `/termin-buchen` to the shared mode contract</name>
  <files>app/page.tsx, app/termin-buchen/page.tsx, lib/home-page.ts</files>
  <read_first>
    - app/page.tsx
    - app/termin-buchen/page.tsx
    - lib/home-page.ts
    - content/booking.ts
    - lib/site-mode.ts
  </read_first>
  <action>Replace inline route-level mode checks with selector-driven state from `lib/site-mode.ts` per D-04, D-05, D-06, D-07, and D-10. On the homepage, prepend the booking CTA only when the mode is `booking`, while leaving configured contact actions visible in both modes. On `/termin-buchen`, render the title, body copy, and visible fallback channels from `content/booking.ts` plus selector output, and remove hardcoded fallback-channel sentences such as `telefonisch, per WhatsApp oder per E-Mail` so the page always matches config.</action>
  <acceptance_criteria>
    - `rg -n 'site\\.booking\\.|booking\\.mode ===' app/page.tsx app/termin-buchen/page.tsx` returns no matches.
    - `rg -n 'from \"@/lib/site-mode\"' app/page.tsx app/termin-buchen/page.tsx` returns matches.
    - `rg -n 'telefonisch, per WhatsApp oder per E-Mail' app/termin-buchen/page.tsx` returns no matches.
    - `rg -n 'from \"@/content/booking\"' app/termin-buchen/page.tsx` returns a match.
  </acceptance_criteria>
  <verify>
    <automated>npm.cmd run test:unit -- tests/phase-1/site-mode.test.ts tests/phase-1/termin-buchen-page.test.tsx; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm.cmd run lint</automated>
  </verify>
  <done>`contact_only` and `booking` behavior now flow from one selector layer, the fallback page is config-driven, and the new tests cover the contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| booking config -> public routes | Public CTA exposure and fallback copy must reflect configured mode and visible channels |
| tests -> route behavior | Regression tests must prove the mode contract, not just selector internals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | E | `app/page.tsx` | mitigate | Only surface a booking CTA when shared selectors say the mode is `booking`; cover both modes in `tests/phase-1/site-mode.test.ts`. |
| T-01-05 | S | `app/termin-buchen/page.tsx` | mitigate | Render fallback copy and visible channel labels from config-driven state so the route cannot spoof unavailable booking or hidden contact paths. |
| T-01-06 | R | `tests/phase-1/*` | mitigate | Add executable tests for selector and route behavior so future changes have auditable evidence when mode gating regresses. |
</threat_model>

<verification>
Run `npm.cmd run test:unit -- tests/phase-1/site-mode.test.ts tests/phase-1/termin-buchen-page.test.tsx`, then `npm.cmd run lint`.
Manually smoke `/` and `/termin-buchen` once in both modes after the automated checks pass.
</verification>

<success_criteria>
The repo has a working Phase 1 unit harness, brochure booking entry visibility is selector-driven, and `/termin-buchen` is a friendly config-driven fallback in `contact_only`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-builder-boundaries-mode-hardening/01-02-SUMMARY.md`
</output>
