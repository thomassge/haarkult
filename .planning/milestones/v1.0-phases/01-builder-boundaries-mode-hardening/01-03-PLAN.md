---
phase: 01-builder-boundaries-mode-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - app/layout.tsx
  - components/blocks/site-footer.tsx
  - app/termin-buchen/page.tsx
  - app/termin-buchen/_components/booking-entry-shell.tsx
  - app/termin-buchen/_lib/booking-entry-content.ts
  - app/admin/page.tsx
  - app/admin/_components/admin-shell.tsx
  - lib/site-mode.ts
  - tests/phase-1/content-boundaries.test.ts
autonomous: true
requirements:
  - BUIL-01
  - BUIL-02
must_haves:
  truths:
    - "Brochure layout and footer use the same shared public action rules as the homepage."
    - "The real booking entry surface is physically isolated under `app/termin-buchen/*` and admin is explicitly isolated under `app/admin/*`."
    - "Brochure components remain free of booking-engine and database imports."
  artifacts:
    - path: "app/termin-buchen/_components/booking-entry-shell.tsx"
      provides: "Non-routable booking-entry presentation component"
      contains: "export function BookingEntryShell"
    - path: "app/admin/page.tsx"
      provides: "Explicit admin route boundary placeholder"
      contains: "export default function"
    - path: "tests/phase-1/content-boundaries.test.ts"
      provides: "Regression checks for brochure/booking separation"
      contains: "describe("
  key_links:
    - from: "app/layout.tsx"
      to: "lib/site-mode.ts"
      via: "shared footer action resolution"
      pattern: "from \"@/lib/site-mode\""
    - from: "components/blocks/site-footer.tsx"
      to: "app/layout.tsx"
      via: "resolved public-action props"
      pattern: "publicActions|actions"
    - from: "tests/phase-1/content-boundaries.test.ts"
      to: "app/page.tsx"
      via: "import-boundary assertions"
      pattern: "app/page.tsx"
---

<objective>
Make the brochure versus booking/admin boundaries explicit in both file structure and shared public-surface wiring.

Purpose: Implement D-08, D-09, and the remaining D-10 drift removal so future booking/admin work cannot leak into brochure components by convenience.
Output: Thin route composition for `/termin-buchen`, an explicit `/admin` boundary placeholder, shared footer/layout action resolution, and a regression test that guards brochure import boundaries.
</objective>

<execution_context>
@C:/Users/neyma/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/neyma/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/neyma/dev/haarkult/AGENTS.md
@C:/Users/neyma/dev/haarkult/CODEX_CONTEXT.md
@C:/Users/neyma/dev/haarkult/.planning/ROADMAP.md
@C:/Users/neyma/dev/haarkult/.planning/phases/01-builder-boundaries-mode-hardening/01-CONTEXT.md
@C:/Users/neyma/dev/haarkult/.planning/phases/01-builder-boundaries-mode-hardening/01-VALIDATION.md
@C:/Users/neyma/dev/haarkult/app/layout.tsx
@C:/Users/neyma/dev/haarkult/components/blocks/site-footer.tsx
@C:/Users/neyma/dev/haarkult/app/termin-buchen/page.tsx
@C:/Users/neyma/dev/haarkult/lib/site-mode.ts

<interfaces>
From components/blocks/site-footer.tsx:
```ts
type SiteFooterProps = {
  brandName: string;
  city: string;
  phone: string;
  email: string;
  bookingHref?: string;
};
```

From app/termin-buchen/page.tsx:
```ts
export default function BookingPage() {}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move booking-entry presentation into route-local modules and create the admin boundary</name>
  <files>app/termin-buchen/page.tsx, app/termin-buchen/_components/booking-entry-shell.tsx, app/termin-buchen/_lib/booking-entry-content.ts, app/admin/page.tsx, app/admin/_components/admin-shell.tsx</files>
  <read_first>
    - app/termin-buchen/page.tsx
    - CODEX_CONTEXT.md
    - .planning/phases/01-builder-boundaries-mode-hardening/01-CONTEXT.md
    - app/page.tsx
  </read_first>
  <action>Refactor `/termin-buchen` so `page.tsx` becomes a thin route composition file and its presentation/copy assembly live in non-routable `_components` and `_lib` files under `app/termin-buchen/*`, per D-08 and D-09. Create `app/admin/page.tsx` and `app/admin/_components/admin-shell.tsx` as an explicit admin boundary placeholder for later protected work; keep the shell brochure-independent and free of `content/home.ts`, `components/blocks/*`, and database or booking-engine imports.</action>
  <acceptance_criteria>
    - `Test-Path app/termin-buchen/_components/booking-entry-shell.tsx` returns `True`.
    - `Test-Path app/termin-buchen/_lib/booking-entry-content.ts` returns `True`.
    - `Test-Path app/admin/page.tsx` returns `True`.
    - `Test-Path app/admin/_components/admin-shell.tsx` returns `True`.
    - `rg -n 'from \"@/components/blocks\"|from \"@/content/home\"|@/db|@/lib/booking' app/admin/page.tsx app/admin/_components/admin-shell.tsx` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>npm.cmd run lint</automated>
  </verify>
  <done>Booking-entry UI is route-local and non-routable, and the admin boundary exists as its own route tree without brochure coupling.</done>
</task>

<task type="auto">
  <name>Task 2: Feed layout and footer from shared public actions and lock the boundary with tests</name>
  <files>app/layout.tsx, components/blocks/site-footer.tsx, lib/site-mode.ts, tests/phase-1/content-boundaries.test.ts</files>
  <read_first>
    - app/layout.tsx
    - components/blocks/site-footer.tsx
    - lib/site-mode.ts
    - .planning/phases/01-builder-boundaries-mode-hardening/01-VALIDATION.md
  </read_first>
  <action>Replace the footer's raw `phone`, `email`, and inline `bookingHref` decision with a resolved public-action contract derived from shared selectors so layout and footer cannot drift from homepage behavior, per D-06 and D-10. Extend `lib/site-mode.ts` only as needed to expose footer-safe public actions. Add `tests/phase-1/content-boundaries.test.ts` that asserts brochure entrypoints do not import `@/lib/booking` or `@/db`, and that layout/footer booking visibility comes from shared selector usage rather than local mode branching.</action>
  <acceptance_criteria>
    - `rg -n 'site\\.booking\\.|bookingHref' app/layout.tsx components/blocks/site-footer.tsx` returns no matches.
    - `rg -n 'from \"@/lib/site-mode\"' app/layout.tsx` returns a match.
    - `Test-Path tests/phase-1/content-boundaries.test.ts` returns `True`.
    - `rg -n '@/lib/booking|@/db' app/page.tsx app/layout.tsx components/blocks/site-footer.tsx` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>npm.cmd run test:unit -- tests/phase-1/content-boundaries.test.ts; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm.cmd run lint; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm.cmd run build</automated>
  </verify>
  <done>Footer and layout consume shared public-action state, brochure files stay free of booking-engine/database imports, and a regression test protects the separation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| brochure layout -> shared public actions | Layout/footer must not invent their own feature exposure rules |
| brochure modules -> booking/admin modules | Public brochure code must stay isolated from operational modules |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | I | `app/layout.tsx`, `components/blocks/site-footer.tsx` | mitigate | Drive footer and layout from shared selector output so public surfaces cannot disclose booking/admin capability that is disabled for the salon. |
| T-01-08 | E | `app/admin/*` | mitigate | Keep `/admin` in its own route tree and avoid brochure imports so later auth work has a clean access-control boundary from day one. |
| T-01-09 | T | brochure entrypoints | mitigate | Add a boundary regression test that fails if brochure files import `@/lib/booking` or `@/db`, preventing operational coupling drift. |
</threat_model>

<verification>
Run `npm.cmd run test:unit -- tests/phase-1/content-boundaries.test.ts`, then `npm.cmd run lint`, then `npm.cmd run build`.
Manually smoke the footer and `/admin` placeholder once after the automated checks pass.
</verification>

<success_criteria>
Public brochure surfaces share one action-resolution path, `/termin-buchen` and `/admin` are physically isolated from brochure blocks, and regression tests guard against booking/admin import leakage.
</success_criteria>

<output>
After completion, create `.planning/phases/01-builder-boundaries-mode-hardening/01-03-SUMMARY.md`
</output>
