---
phase: 01-builder-boundaries-mode-hardening
plan: 04
type: execute
wave: 4
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - content/booking.ts
  - tests/phase-1/termin-buchen-page.test.tsx
autonomous: true
requirements:
  - MODE-03
gap_closure: true
must_haves:
  truths:
    - "The `contact_only` `/termin-buchen` fallback never names a contact channel that is hidden by `fallbackActions`."
    - "Any rendered channel-specific fallback wording comes from selector-driven visible contact kinds, while static fallback step copy stays channel-agnostic."
    - "A regression test fails if `fallbackActions` hides WhatsApp or E-Mail but the rendered fallback still mentions them."
  artifacts:
    - path: "content/booking.ts"
      provides: "Channel-agnostic `contactOnly.steps` copy for the fallback page"
      contains: "Terminwunsch ueber einen der verfuegbaren Kontaktwege senden"
    - path: "tests/phase-1/termin-buchen-page.test.tsx"
      provides: "MODE-03 regression coverage for hidden contact channels"
      contains: "fallbackActions: [\"phone\"]"
  key_links:
    - from: "content/booking.ts"
      to: "app/termin-buchen/_lib/booking-entry-content.ts"
      via: "contactOnly copy consumed by `getBookingEntryContent()`"
      pattern: "booking\\.copy\\.contactOnly"
    - from: "tests/phase-1/termin-buchen-page.test.tsx"
      to: "app/termin-buchen/page.tsx"
      via: "rendered fallback assertions under mocked booking config"
      pattern: "from \"@/app/termin-buchen/page\""
---

<objective>
Close the remaining MODE-03 verification blocker without reopening the completed Phase 1 architecture work.

Purpose: The root cause is an execution and test-coverage miss, not a plan deficiency. Shared selectors and route boundaries are already correct; one static fallback string and one shallow test still allow contact-channel drift.
Output: Channel-agnostic fallback step copy in `content/booking.ts` plus a regression test that mutates `fallbackActions` and proves hidden channels never appear on `/termin-buchen`.
</objective>

<execution_context>
@C:/Users/neyma/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/neyma/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/neyma/dev/haarkult/AGENTS.md
@C:/Users/neyma/dev/haarkult/.planning/ROADMAP.md
@C:/Users/neyma/dev/haarkult/.planning/REQUIREMENTS.md
@C:/Users/neyma/dev/haarkult/.planning/phases/01-builder-boundaries-mode-hardening/01-VERIFICATION.md
@C:/Users/neyma/dev/haarkult/.planning/phases/01-builder-boundaries-mode-hardening/01-02-SUMMARY.md
@C:/Users/neyma/dev/haarkult/.planning/phases/01-builder-boundaries-mode-hardening/01-03-SUMMARY.md
@C:/Users/neyma/dev/haarkult/content/booking.ts
@C:/Users/neyma/dev/haarkult/app/termin-buchen/_lib/booking-entry-content.ts
@C:/Users/neyma/dev/haarkult/tests/phase-1/termin-buchen-page.test.tsx

<interfaces>
From `content/booking.ts`:
```ts
contactOnly: {
  subtitlePrefix: string;
  steps: string[];
}
```

From `app/termin-buchen/_lib/booking-entry-content.ts`:
```ts
export function getBookingEntryContent() {
  const presentationState = getBookingPresentationState(site, booking);
  const fallbackChannels = presentationState.visibleContactKinds.map(
    (kind) => fallbackChannelLabels[kind]
  );
  const subtitle = presentationState.isBookingEnabled
    ? booking.copy.booking.subtitle
    : `${booking.copy.contactOnly.subtitlePrefix} ${formatInlineList(fallbackChannels)}.`;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove static hidden-channel drift from contact-only fallback copy</name>
  <files>content/booking.ts</files>
  <read_first>
    - content/booking.ts
    - app/termin-buchen/_lib/booking-entry-content.ts
    - .planning/phases/01-builder-boundaries-mode-hardening/01-VERIFICATION.md
  </read_first>
  <action>Treat this gap as an execution miss, not a selector redesign. In `content/booking.ts`, replace the first `booking.copy.contactOnly.steps` item at the current hardcoded sentence `Terminwunsch telefonisch, per WhatsApp oder per E-Mail senden` with the exact generic string `Terminwunsch ueber einen der verfuegbaren Kontaktwege senden`. Leave channel naming to the existing selector-driven subtitle and rendered contact actions, which already come from `visibleContactKinds`. Do not add new route branches or new config fields.</action>
  <acceptance_criteria>
    - `rg -n 'Terminwunsch telefonisch, per WhatsApp oder per E-Mail senden' content/booking.ts` returns no matches.
    - `rg -n 'Terminwunsch ueber einen der verfuegbaren Kontaktwege senden' content/booking.ts` returns one match.
    - `rg -n 'subtitlePrefix' content/booking.ts app/termin-buchen/_lib/booking-entry-content.ts` returns matches, confirming the selector-driven subtitle path remains in place.
  </acceptance_criteria>
  <verify>
    <automated>npm.cmd run test:unit -- tests/phase-1/termin-buchen-page.test.tsx</automated>
  </verify>
  <done>The fallback page no longer embeds static WhatsApp or E-Mail wording in its step copy, so hidden contact channels cannot leak through `content/booking.ts`.</done>
</task>

<task type="auto">
  <name>Task 2: Add a regression test that hides channels and proves they stay hidden</name>
  <files>tests/phase-1/termin-buchen-page.test.tsx</files>
  <read_first>
    - tests/phase-1/termin-buchen-page.test.tsx
    - content/booking.ts
    - app/termin-buchen/page.tsx
    - app/termin-buchen/_lib/booking-entry-content.ts
    - .planning/phases/01-builder-boundaries-mode-hardening/01-VERIFICATION.md
  </read_first>
  <action>Refactor `tests/phase-1/termin-buchen-page.test.tsx` so each test can override `@/content/booking` independently with `vi.doMock(...)` after `vi.resetModules()`. Keep the current contact-only rendering assertion, then add a second regression case that sets `mode: "contact_only"` and `fallbackActions: ["phone"]`, renders `BookingPage`, and proves the full fallback stays aligned with that configuration: the subtitle resolves to phone-only wording, the phone action still renders, and the DOM contains no `WhatsApp` or `E-Mail` text or links anywhere on the page.</action>
  <acceptance_criteria>
    - `rg -n 'vi\\.doMock\\(\"@/content/booking\"' tests/phase-1/termin-buchen-page.test.tsx` returns a match.
    - `rg -n 'fallbackActions: \\[\"phone\"\\]' tests/phase-1/termin-buchen-page.test.tsx` returns a match.
    - `rg -n 'queryByText\\(/WhatsApp/i\\)|queryByRole\\(\"link\", \\{ name: /WhatsApp/i \\}\\)' tests/phase-1/termin-buchen-page.test.tsx` returns a match.
    - `rg -n 'queryByText\\(/E-Mail/i\\)|queryByRole\\(\"link\", \\{ name: /E-Mail/i \\}\\)' tests/phase-1/termin-buchen-page.test.tsx` returns a match.
  </acceptance_criteria>
  <verify>
    <automated>npm.cmd run test:unit -- tests/phase-1/termin-buchen-page.test.tsx; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm.cmd run lint</automated>
  </verify>
  <done>The test suite now proves MODE-03 against a changed `fallbackActions` configuration and will fail if hidden channels leak back into rendered fallback copy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| booking config -> fallback page | Public fallback copy must not advertise contact channels that the salon configuration hides |
| regression test -> future copy edits | Future content edits must be caught if they reintroduce hidden-channel wording |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | S | `content/booking.ts` | mitigate | Remove the hardcoded `WhatsApp` and `E-Mail` step sentence so the fallback cannot spoof unavailable channels from static copy. |
| T-01-11 | T | `/termin-buchen` fallback rendering | mitigate | Keep channel-specific wording limited to the existing selector-driven subtitle/actions that already follow `visibleContactKinds`. |
| T-01-12 | R | `tests/phase-1/termin-buchen-page.test.tsx` | mitigate | Add a config-mutation regression test with `fallbackActions: ["phone"]` so hidden-channel drift is reproducible and automatically checked. |
</threat_model>

<verification>
Run `npm.cmd run test:unit -- tests/phase-1/termin-buchen-page.test.tsx` and `npm.cmd run lint`.
Confirm `rg -n 'Terminwunsch telefonisch, per WhatsApp oder per E-Mail senden' content/booking.ts` returns no results after implementation.
</verification>

<success_criteria>
MODE-03 is fully satisfied: `/termin-buchen` in `contact_only` only names configured contact channels, and the regression test proves hidden channels stay absent when `fallbackActions` changes.
</success_criteria>

<output>
After completion, create `.planning/phases/01-builder-boundaries-mode-hardening/01-04-SUMMARY.md`
</output>
