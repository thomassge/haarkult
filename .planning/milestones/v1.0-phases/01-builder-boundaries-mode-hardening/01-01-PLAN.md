---
phase: 01-builder-boundaries-mode-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/site.ts
  - content/booking.ts
  - lib/site-mode.ts
  - lib/home-page.ts
autonomous: true
requirements:
  - BUIL-01
  - BUIL-02
  - MODE-01
  - MODE-02
must_haves:
  truths:
    - "Maintainer can update salon-wide identity, SEO, and contact data in `content/site.ts` without editing booking-rule fields."
    - "Maintainer can switch between `contact_only` and `booking` from one booking-domain config module."
    - "Brochure code has one shared selector contract for mode-aware booking and contact decisions."
  artifacts:
    - path: "content/booking.ts"
      provides: "Booking-domain config, copy, and canonical mode vocabulary"
      contains: "export const bookingModes = [\"contact_only\", \"booking\"]"
    - path: "content/site.ts"
      provides: "Salon-wide brand, contact, hours, socials, and SEO only"
      contains: "export type SiteConfig"
    - path: "lib/site-mode.ts"
      provides: "Pure brochure-safe selectors for booking visibility and fallback contact resolution"
      exports: ["isBookingEnabled", "getBookingEntryHref", "resolveVisibleContactKinds"]
  key_links:
    - from: "content/booking.ts"
      to: "lib/site-mode.ts"
      via: "typed booking config imports"
      pattern: "from \"@/content/booking\""
    - from: "lib/site-mode.ts"
      to: "lib/home-page.ts"
      via: "shared selector consumption"
      pattern: "from \"@/lib/site-mode\""
---

<objective>
Normalize the reusable salon content boundary before public mode-hardening work starts.

Purpose: Implement D-01, D-02, D-03, and the mode-vocabulary decision so later plans stop carrying mixed `booking` versus `online_booking` terminology.
Output: A dedicated `content/booking.ts`, a slimmer `content/site.ts`, and a shared selector contract that brochure code can consume without importing booking internals.
</objective>

<execution_context>
@C:/Users/neyma/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/neyma/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/neyma/dev/haarkult/AGENTS.md
@C:/Users/neyma/dev/haarkult/CODEX_CONTEXT.md
@C:/Users/neyma/dev/haarkult/.planning/ROADMAP.md
@C:/Users/neyma/dev/haarkult/.planning/REQUIREMENTS.md
@C:/Users/neyma/dev/haarkult/.planning/phases/01-builder-boundaries-mode-hardening/01-CONTEXT.md
@C:/Users/neyma/dev/haarkult/.planning/phases/01-builder-boundaries-mode-hardening/01-RESEARCH.md
@C:/Users/neyma/dev/haarkult/content/site.ts
@C:/Users/neyma/dev/haarkult/lib/home-page.ts

<interfaces>
From content/site.ts:
```ts
export type SiteConfig = {
  brand: { name: string; city: string; country: string; logo?: BrandAsset | null; heroImage?: BrandAsset | null };
  contact: { phone: string; fax?: string | null; email: string; whatsapp: string | null; address: {...}; mapsUrl: string };
  legal: { businessName: string; owners: string[] };
  hours: OpeningHour[];
  booking: BookingConfig;
  socials: { instagram: string | null };
  seo: { title: string; description: string };
};
```

From lib/home-page.ts:
```ts
export type ResolvedPageAction = {
  label: string;
  href: string;
  variant: ButtonVariant;
  external: boolean;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract booking-domain config and canonize mode vocabulary</name>
  <files>content/site.ts, content/booking.ts</files>
  <read_first>
    - content/site.ts
    - .planning/phases/01-builder-boundaries-mode-hardening/01-CONTEXT.md
    - .planning/phases/01-builder-boundaries-mode-hardening/01-RESEARCH.md
    - AGENTS.md
  </read_first>
  <action>Create `content/booking.ts` as the only new config module for this phase per D-02 and D-03. Move booking-specific rule fields, fallback contact visibility, and booking-entry copy out of `content/site.ts`, while leaving salon-wide brand, contact, legal, hours, socials, and SEO in `content/site.ts` per D-01. Normalize the internal booking-mode literal to `\"booking\"` instead of `\"online_booking\"` across the touched config exports so the repo stops mixing human-facing and code-facing vocabulary.</action>
  <acceptance_criteria>
    - `rg -n 'export const bookingModes = \\[\"contact_only\", \"booking\"\\]' content/booking.ts` returns a match.
    - `rg -n 'online_booking' content/site.ts content/booking.ts` returns no matches.
    - `rg -n 'confirmationMode|allowStylistSelection|leadTimeHours|maxAdvanceDays|slotStepMinutes|bufferBeforeMinutes|bufferAfterMinutes' content/site.ts` returns no matches.
    - `rg -n 'brand:|contact:|hours:|socials:|seo:' content/site.ts` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>npm.cmd run lint</automated>
  </verify>
  <done>`content/site.ts` is salon-wide only, `content/booking.ts` owns booking-mode state and copy, and the canonical literal used by this phase is `booking`.</done>
</task>

<task type="auto">
  <name>Task 2: Define brochure-safe mode selectors against the new config boundary</name>
  <files>lib/site-mode.ts, lib/home-page.ts</files>
  <read_first>
    - lib/home-page.ts
    - content/booking.ts
    - content/home.ts
    - app/page.tsx
    - app/termin-buchen/page.tsx
  </read_first>
  <action>Create `lib/site-mode.ts` with pure selectors that read the new `site` plus `booking` config and expose brochure-safe decisions: whether booking is enabled, the booking entry href, the visible fallback contact kinds, and a stable presentation-state helper for brochure surfaces. Update `lib/home-page.ts` to consume these selectors instead of branching on raw booking fields, but preserve the existing `ResolvedPageAction` output contract so 01-02 can rewire routes without more interface churn. Do not import `lib/booking/*` or `db/*` into this selector layer per D-08 and D-10.</action>
  <acceptance_criteria>
    - `rg -n 'export function isBookingEnabled|export function getBookingEntryHref|export function resolveVisibleContactKinds' lib/site-mode.ts` returns matches.
    - `rg -n 'from \"@/lib/site-mode\"' lib/home-page.ts` returns a match.
    - `rg -n 'site\\.booking\\.' lib/home-page.ts` returns no matches.
    - `rg -n '@/lib/booking|@/db' lib/site-mode.ts lib/home-page.ts` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>npm.cmd run lint</automated>
  </verify>
  <done>A single brochure-safe selector layer exists, `lib/home-page.ts` depends on it, and later public surfaces can adopt it without redefining mode logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer config -> selector layer | Untrusted repo edits to mode or contact visibility must not create inconsistent public behavior |
| selector layer -> brochure routes | Public surfaces depend on these helpers for feature exposure decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | `content/site.ts`, `content/booking.ts` | mitigate | Split config ownership so booking rules cannot silently drift inside salon-wide fields; enforce typed exports and one canonical mode literal. |
| T-01-02 | S | `lib/site-mode.ts` | mitigate | Centralize booking-enabled detection and href resolution so brochure routes do not spoof enabled booking from local checks. |
| T-01-03 | I | `lib/site-mode.ts` | accept | This layer exposes only public visibility state, not secrets or PII; risk stays low if it never imports `lib/booking/*` or `db/*`. |
</threat_model>

<verification>
Run `npm.cmd run lint` after each task.
Spot-check with `rg` that `online_booking` is gone from the touched config and selector files before starting 01-02.
</verification>

<success_criteria>
`content/booking.ts` exists, `content/site.ts` no longer owns booking-rule fields, `booking` is the canonical internal literal, and brochure-safe mode selectors exist in one shared helper module.
</success_criteria>

<output>
After completion, create `.planning/phases/01-builder-boundaries-mode-hardening/01-01-SUMMARY.md`
</output>
